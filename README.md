# aws-localstack
# Dependencies
1. Docker
2. Docker Compose
3. Kubectl
4. Kind
5. Terraform
6. AWS CLI
7. Eksctl
8. Helm

# Setup
1. Install deps using `./install-dev-tools`
2. Start localstack using `docker-compose up -d`
