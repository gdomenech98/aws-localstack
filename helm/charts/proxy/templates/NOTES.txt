NOTES.txt
----------
Thank you for installing {{ .Chart.Name }}!

{{- if .Values.ingress.enabled }}
Your application can be accessed at:

{{- range .Values.ingress.hosts }}
  {{- $host := .host }}
  {{- range .paths }}
    {{- if eq .service.name "frontend" }}
Frontend: http://{{$host}}{{.path}}
    {{- end }}
    {{- if eq .service.name "api" }}
API: http://{{$host}}{{.path}}
    {{- end }}
  {{- end }}
{{- end }}

{{- else }}
No ingress configured. Use 'kubectl port-forward' to access your services.
{{- end }}


Run the following to get the pod status:
  kubectl get pods -l app={{ .Chart.Name }}
